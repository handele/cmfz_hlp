<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="com.baizhi.hlp.dao.ChapterDAO">

	<!-- 添加章节 -->
	<insert id="insert">
		insert into chapter values(#{id},#{title},#{size},#{url},#{duration},#{pub_date},#{album_id})
	</insert>
	
</mapper>
